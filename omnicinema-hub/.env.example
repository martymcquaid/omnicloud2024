# Backend API base URL (optional).
# Use when the browser cannot reach the backend at localhost (e.g. you open the app from another machine).
# Examples:
#   Same machine: leave unset or set to http://localhost:10858/api/v1
#   From another machine: set to the server URL, e.g. http://YOUR_SERVER_IP:10858/api/v1
# VITE_API_URL=http://localhost:10858/api/v1
